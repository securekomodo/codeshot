Incoming reading
├── Sensor known?
│   ├── yes → validate
│   └── no  → quarantine
├── Within range?
│   ├── yes → store
│   └── no  → flag + alert
└── Alert threshold crossed?
    ├── yes → debounce → notify
    └── no  → done
