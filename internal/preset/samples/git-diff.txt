diff --git a/api/alerts/notify.go b/api/alerts/notify.go
--- a/api/alerts/notify.go
+++ b/api/alerts/notify.go
@@ -12,6 +12,8 @@ func Notify(a Alert) error {
 	if a.Severity < Warning {
-		return nil
+		log.Printf("skip %s: below threshold", a.ID)
+		return ErrBelowThreshold
 	}
 	return send(a)
